version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DARKRECONX_API_TOKEN=devtoken

  cli:
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: ["asr", "--help"]
